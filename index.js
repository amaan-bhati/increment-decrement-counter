var input=document.getElementById("input")  
     function increase(){  
       input.value=eval(input.value)+1//upper limit
       //changed different values but the functionality remained the same and hence the functionality is fit 
     }  
     function decrease(){  
       input.value=eval(input.value)-1//lower limit
     }  